#Generates random string.
import random
#Set dice as random.randint which returns random integers from low to high.
dice = random.randint(1, 6)
#Print dice num = the random number from 1 to 6.
print "dice num =", dice
